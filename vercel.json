{
  "version": 2,
  "builds": [
    { "src": "src/app/api/**/*.ts", "use": "@vercel/node" },
    { "src": "next.config.ts", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/src/app/api/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
